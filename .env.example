HF_TOKEN=hf_your_token_here

# Cluster vars — used by `make finetune-sync` to push config edits to the cluster
# without committing. Copy this file to .env and replace the placeholder values.
CLUSTER_HOST=user@goethe.hhlr-gu.de
CLUSTER_REPO_DIR=/work/your_group/your_username/llm-fine-tune
